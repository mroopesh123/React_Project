function Propss({name,age,user})
{
    return (
        <div>
            
            <h1>Welcome to Props Mr. {name}</h1>
            <h1>and your age is {age}</h1>
            <h2>By using objects : name: {user.name} and Age: {user.age}</h2>
        </div>
    );
}
export default Propss
